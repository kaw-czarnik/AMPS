{
  "name": "amps",
  "version": "1.0.0",
  "description": "Sistema de gerenciamento de estacionamento baseado em grafo: mapeia o pátio como um grafo de vagas e vias, localiza o cliente e recomenda a melhor vaga livre com roteamento por caminho mínimo.",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present && tsc --noEmit -p tools/tsconfig.json",
    "pretest": "npm run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:e2e": "vitest run --config vitest.e2e.config.ts",
    "seed:patios": "tsx tools/semear-patios.ts",
    "dev": "trap 'kill 0' EXIT INT TERM; npm run dev -w @amps/api & npm run dev -w @amps/web & wait"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gengibrepower/AMPS.git"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/gengibrepower/AMPS/issues"
  },
  "homepage": "https://github.com/gengibrepower/AMPS#readme",
  "devDependencies": {
    "@types/node": "^26.2.0",
    "dotenv": "^17.4.2",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
